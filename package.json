{
  "name": "app-de-notas",
  "version": "1.0.0",
  "author": "Brayan Duran Velasquez",
  "description": "Una aplicacion para guardar sus notas.",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "generate": "nuxt build && nuxt export",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/pwa": "^3.0.0-beta.20",
    "alertifyjs": "^1.13.1",
    "nuxt": "^2.13.0"
  },
  "devDependencies": {
    "node-sass": "^4.14.1",
    "push-dir": "^0.4.1",
    "sass-loader": "^8.0.2"
  }
}
